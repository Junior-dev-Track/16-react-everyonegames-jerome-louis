import Logo from './Logo'
import SearchBar from './SearchBar';
const Header = () => {
    return (
        <div>
            <Logo />
            <SearchBar />
        </div>
    );
};

export default Header;